import React from 'react';
import { Tabs, TabList, Tab, TabPanel } from "react-tabs";

const Repositories = () => {
    return (
        <div>
            
        </div>
    );
}

export default Repositories;